express-hbs installation

type `npm i express-hbs` in the terminal/console

express-hbs => Express handlebars template engine with multiple layouts, blocks and cached partials.

then copy the `app.js` file