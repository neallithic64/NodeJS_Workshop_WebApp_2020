{{> navbar }}
<div class="row">
    <div class="col">
        <h1>
            Hello {{ email }}
            <br>
            {{#if checkMe}}
                {{ password }}
            {{else}}
                No peeking
            {{/if}}
        </h1>
    </div>
</div>